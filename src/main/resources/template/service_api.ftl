package ${package_path}.service;

import ${crudService_path};
import ${package_path}.model.${class_name};
import ${package_path}.param.${param_name};
import com.github.pagehelper.PageInfo;

import java.util.List;

/**
 * @author ${author}
 * @date ${sysDate?date}
 */
public interface ${class_name}Service extends CrudService<${class_name}, ${param_name}> {

    // ------------------------- 自定方法 -------------------------


}